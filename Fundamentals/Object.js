const obj={}
obj.names="Cellphone" //Dynamically created value, this is essentially a dictionary
obj["Names"]="no" //when doing square braces, key is a string, otherwise just name the key

const obj2={
    names:"camis polo", //inside {} use comma between statements
    price: 79.90
}
console.log(obj,obj2)