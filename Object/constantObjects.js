const person={Name: "Ari"} //Person is constnat, which means the address of the object is constant
//the reference is constant, but the object itself can be changed
person.height="5 foot 3"
console.log(person)